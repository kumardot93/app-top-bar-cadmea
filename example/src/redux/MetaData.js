const MetaData = (
	state = {
		base: 'http://localhost:8000',
		profile_url: 'http://localhost:8000/user/api/profile',
		fetch_credentials: 'include',
		media_url: 'http://localhost:8000/media/'
	},
	action
) => {
	return state;
};

export default MetaData;
